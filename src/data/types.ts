export interface TimeDurationInputAttr {
  minutes?: string;
  seconds?: string;
  milliseconds?: string;
  plcMinutes: string;
  plcSeconds: string;
  plcMilliseconds: string;
  label: string;
  index?: number;
}
